apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: webapp
  name: test
spec:
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
      - name: test
        image: nginx
        resources:
        ports:
        - containerPort: 5000
